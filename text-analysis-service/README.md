# Tests

`cd text-analysis-service && python3 -m pytest tests/unit/test_embedding.py -v --tb=short`


# Local development using AWS SAM 

1. Install `sam`. See https://docs.aws.amazon.com/serverless-application-model/latest/developerguide/install-sam-cli.html
2. 